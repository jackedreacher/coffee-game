using UnityEngine;

// Taken straight out of the fridge, finished. Nothing is prepared and nothing
// waits, so there is no raw version and no ingredientOnly flag -- a can is
// something a customer can be handed as it comes
public class Drink : SpawnableFood
{
}
