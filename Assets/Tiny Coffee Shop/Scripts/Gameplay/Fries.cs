using UnityEngine;

// Comes out of the fryer finished. Nothing goes in, so there is no raw version
// to guard against and no ingredientOnly flag -- a portion of fries is a meal on
// its own and may go straight to a customer
public class Fries : SpawnableFood
{
}
