Welcome back guys! In this lesson we are going to handle saving and loading our workers

unlocked state. So let's jump right in. Okay, so in our HR manager here we want this to implement

the IWantToBeSaved interface and of course it needs to use tabseal.sigil. Okay, so let's now

implement the interface. We've got our save and load method which is really cool. Let's

start with the save method because it's the the simplest one. Inside of here what do we want to

save? Well, we basically just want to save the worker levels because as I've told you previously

if the worker is still locked the level or its level is going to be minus one. The worker level

is going to be minus one so negative otherwise positive and this is going to help us determine

if the worker has been unlocked and at the same time its level which is really convenient. So for

i from zero to workerLevels.length what do we want to do? Well, sigil.save we want to save this

and workerLevelKey so workerLevelKey plus the index and save our workerLevels at index i.

Like so. This is our save method. Really simple. Let's create this key really quickly. Okay, so

data private constant string workerLevelKey and let's do something like maybe worker

underscore level underscore. Perfect. So now we can safely implement our load method.

Boom! So loading here simply means grabbing our worker levels generating the containers and so on.

So we've done this at start. We are not going to do that at start. We are going to do that

in the load method now because we have it. So workerLevels equals this and before generating

the containers we need to grab our workerLevels. So for i from zero to workerLevels.length

we are going to try and grab it from sigil. So if sigil.tryload this and the workerLevelKey

plus i out object underscore workerLevel. If we've successfully loaded a data or a value,

an object from this key, well we are going to store it in our workerLevels. So workerLevels

at index i will be equals to integer cast of our data, the one that we've found.

Otherwise our workerLevel at index i is going to be negative. Minus one, minus two, minus three,

you decide here. Because at start we are going to grab the maximum value if you remember well

from the... yeah right here. Level equals matf.maximum between these two values.

And after that we can definitely generate the worker containers. We can also initialize the

workerManagers with the data that we've unlocked or grabbed. Because now that we've got the worker

levels some of the workers might be unlocked. So the workerManagers needs to spawn these workers

and to know if he needs to spawn these workers he will need the workerData and the workerLevels.

Got it? So let's do workerManager.initialize with the workerDatas and the workerLevels.

There you go. So let's create this method now. Boom. Public and remove this. Inside of here it's

going to be quite simple. So I think it can be a nice quick, really quick exercise. So try

and do that. Try and spawn the workers if they need to be spawned. You can pause the video.

Okay. Here is the solution. So let's loop through all of our workerLevels. If the workerLevels

at index i is greater than zero we need to spawn it. A worker, sorry. Not it. But if it's less than

zero we should not do anything. So let's just continue. Otherwise, again, let's spawn the worker.

Boom. What worker should we spawn? Well, workerDatas at index i, I guess, and workerLevels

at index i. And you now see that the workerLevel here was really relevant. Okay. So just like that

we should be able to spawn our workers here. Nothing to specify in here. So let's just hit play.

All right. Let's display our panel here. Let's unlock the first, maybe, and the Jawad too. Yeah.

These two workers. Let's exit. Do we save when unlocking the workers or not? Oh, I forgot that.

I forgot about that. HRManager. Load. Save. Do we call the save? No, we don't. So let's uncomment

this. Really cool. So now we will be able to save our data when unlocking the worker and we can

finally try this out. So let's hit play again. All right. Let's unlock them again. Boom and boom.

Exit. Quit the play mode. Come back. And our workers are here. Yay. And they are moving.

Awesome. Really cool. Nice. So that was quick but really important. In the next one, let's work on

these three other methods that we have. We still need to implement them. So see you in the next one.