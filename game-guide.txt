== COOKED-FAST GAME DEV GUIDE ==
Unity 6000.3.16f1 | C# | Isometric Cafe Tycoon
Last updated: Lesson 59 in progress

== ARCHITECTURE ==
- Abstract class pattern: SpawnableFood (abstract) -> CoffeeCup, Pizza
- Plateau system: universal stacking tray used everywhere (stations, player, drop zones, future: customers, tables, workers)
- Push/Pop pattern: Station.Pop() -> Plateau.Pop() -> FoodPosition.Pop() (top-down delegation chain)
- Ability system: player/workers have modular abilities (HoldFoodAbility, future: HoldDishAbility, PayAbility, NavigationAbility)
- PlayerDetector: OnTriggerStay routes to correct ability via TryGetComponent

== FILE STRUCTURE ==
Scripts/Gameplay/     -> SpawnableFood.cs, CoffeeCup.cs, Pizza.cs, Plateau.cs, FoodPosition.cs
Scripts/Stations/     -> FoodSpawnerStation.cs, FoodDropZone.cs, FoodServingStation.cs(RequireComponent FoodServingCustomerManager), FoodServingCustomerManager.cs
Scripts/TableStuff/   -> TableSet.cs, Chair.cs
Scripts/Customer/     -> Customer.cs, CustomerAnimator.cs
Scripts/Player/       -> PlayerController.cs(abstract), JoystickPlayerController.cs, PlayerAnimator.cs, PlayerDetector.cs
Scripts/Abilities/    -> HoldFoodAbility.cs, NavigationAbility.cs, HoldDishAbility.cs, PayAbility.cs
Scripts/Managers/     -> CurrencyManager.cs, GameManager.cs, CustomerManager.cs, TableManager.cs, ProgressionManager.cs, HRManager.cs
Scripts/Stations/     -> (also) DeskStation.cs
Scripts/WorkerStuff/  -> WorkerManager.cs, Worker.cs, WorkerTask.cs, SubTask.cs, MoveToSubTask.cs, WaitForConditionSubTask.cs, TaskRequest.cs, TaskRequester.cs, FillStationPlateauRequest.cs, FillStationPlateauTask.cs, ServeCustomersRequest.cs, ServeCustomersTask.cs
Scripts/Gameplay/     -> LockedElement.cs, CashFile.cs, CharacterStats.cs (+ static WorkerUtilities in the same file)
Scripts/Utilities/    -> Constants.cs (TimeToConsumeFood=1f), ArcAnimator.cs
Scripts/UI/           -> MobileJoystick.cs, UIWorkerContainer.cs, UIWorkerStat.cs, UIUpgradeBlob.cs
Scripts/Data/         -> WorkerDataSO.cs, BaseCharacterStatsSO.cs (ScriptableObjects)
Prefabs/Gameplay/     -> Cup, Pizza, Plateau, CoffeeStation
Prefabs/Characters/   -> Customer
Prefabs/TableStuff/   -> SimpleTableSet, Chair
Existing libs: Sijil(save system), NaughtyAttributes, LeanTween, TotalJSON, Tabsil utilities

== KEY CLASSES ==

SpawnableFood (abstract MonoBehaviour):
  - float CleanYOffsetOnPlateau (serialized, per-food stacking height)
  - future: dirty offset, dirty mesh swap

FoodPosition (MonoBehaviour):
  - bool IsEmpty (init in Awake, not Start - critical for runtime-created positions)
  - SpawnableFood food (stored reference)
  - Push(SpawnableFood): reparent + localPos=zero, set isEmpty=false
  - Pop(): return food, null ref, set isEmpty=true

Plateau (MonoBehaviour):
  - Transform foodPositionsParent (serialized)
  - int maxCapacity (serialized)
  - bool IsFull, bool IsEmpty (properties)
  - float positionsYOffset (set from food.CleanYOffsetOnPlateau)
  - Push(food): find first empty pos -> push -> rearrange -> check full -> create new pos or mark full
  - Pop(): get last full pos -> pop -> check empty -> return food
  - GetFirstEmptyFoodPosition(): loop 0->n
  - GetLastFullPosition(): loop n->0 (grab from top)
  - GetFirstFullPosition(): loop 0->n (empty check after pop)
  - RearrangeFoodPositions(food): set all positions to Vector3.up * i * offset
  - CreateNewFoodPosition(): new GameObject + AddComponent, parent to foodPositionsParent, calc position from bottom sibling

FoodSpawnerStation (MonoBehaviour):
  - SpawnableFood spawnableFoodPrefab (serialized)
  - Plateau plateau (serialized)
  - float spawnDelay (serialized), float spawnTimer
  - Update -> HandleSpawnTimer -> TrySpawnFood -> SpawnFood
  - SpawnFood(): Instantiate prefab as child of station, then plateau.Push
  - Pop(): delegates to plateau.Pop()

FoodDropZone (MonoBehaviour):
  - Plateau plateau (serialized)
  - IsFull -> plateau.IsFull
  - Push(food) -> plateau.Push(food)
  - Pop() -> plateau.Pop() (public)
  - GetFirstFullPosition() -> plateau.GetFirstFullPosition() (public)

FoodServingStation (MonoBehaviour, RequireComponent FoodServingCustomerManager):
  - FoodDropZone dropZone (serialized)
  - float servingDelay (serialized, ~0.3), float servingTimer
  - int workerCount (tracks how many workers/players inside trigger)
  - OnTriggerEnter/Exit: check PlayerDetector, increment/decrement workerCount (min 0)
  - OnTriggerStay: if workerCount>0 -> HandleFoodServing
  - [SerializeField] TableManager tableManager
  - HandleFoodServing: timer check -> IsCustomerReadyToTakeFood -> NeedsMoreFood check -> GetFirstFullPosition -> ServeFood
  - Pop(): dropZone.Pop()
  - ServeFood: PeekFirstCustomer -> Pop food -> CollectFood -> if NeedsMoreFood return, else DequeueCustomer
  - DequeueCustomer: if !tableManager.IsAnyTableAvailable return -> customerManager.Dequeue -> tableManager.HandleCustomerServed

FoodServingCustomerManager (MonoBehaviour):
  - Transform spawnPoint, Transform queueStartPoint (serialized)
  - int maxCustomers, Vector3 queueSpacing (serialized)
  - Vector2Int minMaxCustomerFoodCount (serialized, e.g. (1,4))
  - Queue<Customer> customers
  - SpawnNewCustomer: Random.Range(min, max+1) foodCount, Initialize(foodCount, targetPos, -queueSpacing.normalized)
  - PeekFirstCustomer(): returns customers.Peek()
  - Dequeue(): customers.Dequeue, then loop remaining customers -> GoTo(GetTargetCustomerPosition(i)) to move queue forward
  - GetTargetCustomerPosition(index): queueStartPoint.position + queueSpacing * index
  - IsCustomerReadyToTakeFood(): customers.Count>0, Peek first, distance check <0.1f (y=0 both sides)
  - GetLastCustomerPosition: queueStartPoint.position + queueSpacing * (count-1)

PlayerDetector (MonoBehaviour, RequireComponent HoldFoodAbility):
  - caches HoldFoodAbility in Awake
  - OnTriggerStay: TryGetComponent for FoodSpawnerStation or FoodDropZone, routes to ability

HoldFoodAbility (MonoBehaviour):
  - Plateau plateau (serialized, player's plateau)
  - grab/drop timers with 0.1s delay
  - HandleFoodSpawnerStation: check full -> timer -> station.Pop -> enable plateau -> plateau.Push
  - HandleFoodDropZone: check plateau active -> check dropzone full -> timer -> plateau.Pop -> dropZone.Push -> if empty disable plateau

PlayerAnimator:
  - switches between Idle/Walk and IdleWithPlateau/WalkWithPlateau based on plateau.activeInHierarchy

Customer (MonoBehaviour):
  - enum State { Idle=0, Walking=1, Drinking=2 }
  - CustomerAnimator animator, NavigationAbility navigationAbility (serialized)
  - Plateau plateau (serialized, customer's tray)
  - int foodNeededCount (property FoodNeededCount), int foodTakenCount (property FoodTakenCount), Vector3 finalFacing, Action reachedDestinationCallback
  - CollectFood(food): enable plateau, push food, foodTakenCount++
  - NeedsMoreFood(): return foodTakenCount < foodNeededCount
  - Initialize(foodCount, targetPos, finalFacing): stores values, calls GoToThen(pos, FaceFinalFacing)
  - GoToThen(public, pos, callback): stores callback, calls GoTo
  - GoTo(public): TryGoTo -> StartWalkingState
  - ReachDestination: StartIdleState -> invoke+null callback
  - FaceFinalFacing: animator.Face(finalFacing)
  - HandleWalkingState: HasReachedDestination->ReachDestination, IsMoving->ManageAnimations(velocity), else->StartIdleState

NavigationAbility (MonoBehaviour, RequireComponent NavMeshAgent):
  - NavMeshAgent agent (cached in Awake)
  - TryGoTo(targetPosition): y=0, CalculatePath, if reachable SetPath+isStopped=false, return bool

CustomerAnimator (MonoBehaviour):
  - same as PlayerAnimator (copied), will add sitting animation later
  - Animator animator, GameObject plateau (serialized)
  - ManageAnimations(moveVector, moveSpeed)

CustomerManager (Singleton MonoBehaviour):
  - static Instance (set in Awake)
  - Customer customerPrefab (serialized)
  - Pop(Vector3 spawnPosition): Instantiate customer at position, child of manager

FoodServingStation (MonoBehaviour):
  - empty for now, will handle serving food to customers

FoodServingCustomerManager (MonoBehaviour):
  - Transform spawnPoint, Transform queueStartPoint (serialized)
  - int maxCustomers (serialized)
  - Vector3 queueSpacing (serialized, e.g. (1,0,0) for X-axis spacing)
  - Queue<Customer> customers
  - Start -> InvokeRepeating SpawnNewCustomer every 1s
  - SpawnNewCustomer: check count < max -> Pop -> enqueue -> GetLastCustomerPosition -> Initialize
  - GetLastCustomerPosition: queueStartPoint.position + queueSpacing * (count-1)

== SCENE HIERARCHY ==
Joystick Controller
--- ENVIRONEMENT --- (cameras, lights, ground, decorations)
--- GAMEPLAY --- (Main Ground, Service Ground, Player, Walls)
--- UI --- (Main Canvas, Joystick, Currency)
--- MANAGERS --- (Sijil, Game Manager, Currency Manager)
Coffee Station (FoodSpawnerStation + BoxCollider + trigger BoxCollider)
  |- Station Renderer, Coffee_Machine
  |- Plateau (maxCap:7) -> Anim -> Renderer + Positions -> FoodPosition
Coffee Cashier Station (FoodServingStation + FoodServingCustomerManager + BoxCollider)
  |- Station Renderer, Cashier Machine
  |- Drop Zone (FoodDropZone + trigger BoxCollider)
       |- Plateau (maxCap:500) -> Anim -> Renderer + Positions -> FoodPosition
Simple Table Set (TableSet script + NavMeshObstacle + BoxCollider)
  |- Round Table (model + palette material)
       |- Plateau (maxCap:~7) -> Positions -> FoodPosition
  |- Chair (prefab, Chair script + NavMeshObstacle(carve) + BoxCollider)
  |- Chair (duplicate, rotated 180)
Table Manager (TableManager script, tables[] from GetComponentsInChildren)
  |- Simple Table Set (child of Table Manager)
       |- Chair (isEmpty, customerTargetPoint, customerTargetWalkPoint serialized)
Customer Manager (singleton, spawns customers on demand)
CustomerSpawnPoint (empty transform, where customers appear)
Player has: CharacterController, PlayerDetector, HoldFoodAbility, HoldDishAbility, PayAbility, PlayerAnimator
  |- Plateau (child of character model, starts disabled)
Worker has: CharacterController, PlayerDetector, NavigationAbility, HoldFoodAbility, HoldDishAbility, CustomerAnimator
  |- Plateau (child of character model, starts disabled)

== PATTERNS & GOTCHAS ==
- Awake vs Start: FoodPosition uses Awake for isEmpty init (runtime AddComponent needs immediate init)
- Plateau starts disabled on player, enabled on first food grab, disabled when empty
- All models use single "palette" material with UV color selection
- Trigger colliders need: one side has Collider+Rigidbody(or CharacterController), other has trigger Collider
- Prefab renderer localPosition must be zero (asset imports may have offset - always check)
- Timer pattern: if(timer < delay) { timer += deltaTime; return; } timer = 0; // proceed
- Start() sets grabFoodTimer = delay so player can grab immediately on game start

== PROGRESS ==
[x] Lesson 1: Project setup, Cinemachine, Cup prefab, SpawnableFood/CoffeeCup, FoodSpawnerStation stub, Plateau/FoodPosition stubs
[x] Lesson 2: Spawn timer, Plateau Push/full/dynamic position creation, FoodPosition Push, Pizza variant
[x] Lesson 3: PlayerDetector, HoldFoodAbility, Pop chain (Station->Plateau->FoodPosition), player collects cups
[x] Lesson 4: Grab/drop timers, FoodDropZone, Cashier Station, Plateau IsEmpty, player drops cups at drop zone
[ ] Lesson 5: (skipped in session)
[x] Lesson 6: Customer prefab, CustomerAnimator, CustomerManager singleton, FoodServingStation, FoodServingCustomerManager with Queue, spawn customers at cashier station
[x] Lesson 7: NavMesh Surface, NavMesh Agent on customer, navigation settings, ignore navmesh layer for player
[x] Lesson 8: NavigationAbility, Customer state machine, queue system (spacing + start point), customer walks to cashier
[x] Lesson 9: Customer animations, velocity-based walk/idle, NavMesh hasReachedDestination/isMoving, Lerp rotation
[x] Lesson 10: GoToThen callback, finalFacing, Face(), angular speed=0 fix
[x] Lesson 11: FoodServingStation trigger/worker/timer, foodNeededCount, IsCustomerReadyToTakeFood distance check, FoodDropZone.GetFirstFullPosition, Plateau.GetFirstFullPosition public
[x] Lesson 12: Serve food to customer - Pop from drop zone, CollectFood (enable plateau + push), NeedsMoreFood check, DequeueCustomer
[x] Lesson 13: Table setup - SimpleTableSet (round table + 2 chairs + plateau), Chair prefab, NavMesh Obstacles (carve), box colliders, TableSet/Chair/TableManager empty scripts
[x] Lesson 14: TableManager (IsAnyTableAvailable, GetFirstCleanEmptyTable, HandleCustomerServed->random pos), TableSet.IsFull, FoodServingStation checks table before dequeue, queue moves forward on dequeue, Customer.GoTo public
[x] Lesson 15: TableManager.HandleCustomerServed->table.AcceptCustomer, TableSet(chairs[], AcceptCustomer, GetFirstEmptyChair, CheckIfFull, HandleCustomerReachedChair stub), Chair(isEmpty, IsEmpty, MarkAsOccupied, customerTargetWalkPoint->CustomerTargetWalkPosition), Customer.GoToThen public
[x] Lesson 16: TableSet(List<Customer>, [SerializeField]plateau, HandleCustomerReachedChair->add+PushCustomer+pop loop to table plateau), Chair(PushCustomer: store customer, SetParent, SitDown), Customer(Pop, SitDown, DisableNavigation, StartDrinkingState), NavigationAbility.Disable, CustomerAnimator.PlaySitDownAnimation(isSitting+Play"Sit"+Face)
[x] Lesson 17: Constants(TimeToConsumeFood=1f), per-chair timer (eatTimer=foodCount*TimeToConsumeFood in Chair), Chair.FireCustomer->Pop+HandleFiredCustomer+Messup+OnCustomerLeft, TableSet.OnCustomerLeft(all empty?->isDirty+MarkAsDirty), CustomerManager(customerExitPoint,HandleFiredCustomer->GetUpAndGo->Destroy), Customer.GetUpAndGo(Enable+GoToThen), NavigationAbility.Enable(+warp commented), TableManager.GetFirstCleanEmptyTable checks IsDirty
[x] Lesson 18: SpawnableFood(MeshFilter+dirtyMesh+dirtyYOffset+IsDirty+MarkAsDirty), FoodPosition(DisplayFood,MarkAsDirty->food.MarkAsDirty), Plateau(lastFoodPushed, MarkAsDirty(count) skip dirty, RearrangeFoodPositionsPerFood per-food offset), Chair(render Transform+Messup random ±60Y)
[x] Lesson 19: SpawnableFood(MeshRenderer+IsVisible+Display+Hide), FoodPosition(IsFoodVisible,HideFood,DisplayFood->food.Display), Plateau(HideNextFood top-down+rearrange, RearrangeFoodPositionsPerFood handles hidden cups at zero), TableSet(activeCustomerCount, foodTimer, foodConsumed, HandleFoodTimer, HideNextFood, reset on all-left)
[x] Lesson 20: HoldDishAbility(plateau,CanCollectDishes,CollectDishes), PlayerDetector+TableSet trigger->HandleTableTriggered(IsDirty+HoldDishAbility+CanCollect->GetCleanedBy), TableSet.GetCleanedBy(PopAll+disable plateau+CollectDishes+Fix chairs+isDirty=false), Plateau(IsDirty, Push sets isDirty if food dirty, PopAll returns array+resets), Chair.Fix()->render.localRotation=identity
[x] Lesson 21: Plateau.Push dirty bypass (isDirty||childCount<maxCap -> CreateNewPosition), occupiedPositions cap (count full positions; if >=maxCap -> isFull=true), HoldFoodAbility.HandleFoodDropZone: if plateau.IsDirty return (can't drop dirty at drop zone)
[x] Lesson 22: Trash.cs (OnTriggerEnter->HoldDishAbility.HasDishes->PopAll->Destroy all), HoldDishAbility(HasDishes property, PopAll=plateau.PopAll+disable)
[x] Lesson 23: Worker.cs (empty stub), WorkerManager.cs (List<Worker> workers serialized, Update->HandleRequests, HandleRequests comments: pending? idle? assign+remove)
[x] Lesson 24: WorkerManager singleton+pendingRequests+RegisterRequest, TaskRequest(abstract,Sender+guid+priority+constructors), TaskRequester(CreateTaskRequest->RegisterRequest), GuidGenerator — Tabsil'de zaten var (GUID property), FoodServingStation(RequireComponent GuidGenerator, taskRequester+foodServedPrefab fields, requestTimer->CheckRequests->HasEnoughFood/EmitRequest stub), FoodDropZone.FoodCount, Plateau.GetFoodCount
[x] Lesson 25: FillStationPlateauRequest(guid+food+dropZonePosition+priority=70), FoodDropZone.WorkerTargetPosition(workerTargetPoint transform), FoodServingStation.EmitRequest impl, TaskRequester(duplicate check by guid+type, ClearRequest loop-backwards)
[x] Lesson 26: WorkerManager.HandleRequests impl (pendingRequests check, HandleRequest, HandleFillStationPlateauRequest: FindObjectsByType, potentialStations by FoodType, GetRandom, create FillStationPlateauTask, worker.AssignTask), FoodSpawnerStation(FoodType=spawnablePrefab.GetType, WorkerTargetPosition), FillStationPlateauTask stub
[x] Lesson 27: WorkerTask(abstract, subTasks list, currentSubTaskIndex, worker+request cache, Start/Update/Complete), SubTask(abstract, IsComplete, Start/Update abstract), MoveToSubTask(destination, GoTo+HasReachedDestination), FillStationPlateauTask inherits WorkerTask + MoveToSubTask for food spawner, TaskRequester.ClearRequest
[x] Lesson 28: Worker([RequireComponent NavigationAbility], State enum, Awake caches nav+holdFood, Update calls task.Update, AssignTask+GoTo+MarkAsBusy+CompleteTask, HasReachedDestination+IsPlateauFull/Empty/Dirty props), HoldFoodAbility(Plateau property+IsPlateauEmpty/Full/Dirty), WaitForConditionSubTask(Func<bool>), FillStationPlateauTask 4 subtasks complete, FoodServingStation HandleFoodServing bug fix (customer not needing food → dequeue)
[x] Lesson 29: Worker animations (CustomerAnimator + state machine: Idle/PerformingTask), ServeCustomersRequest(guid+workerTargetPos+dropZone+priority=40), ServeCustomersTask(MoveTo+WaitForCondition(foodCount<3)), FoodServingStation(workerServingTargetPoint, CanSendServeCustomersRequest: workerCount<=0&&customerReady&&hasEnoughFood, EmitServeCustomersRequest, OnTrigger detects Worker too), WorkerManager handles ServeCustomersRequest→ServeCustomersTask, [SerializeReference] on pendingRequests+TaskRequester.requests, [System.Serializable] on TaskRequest+subclasses
[x] Lesson 30: WorkerManager idle check (CurrentTask==null loop, return if none idle), CleanTableRequest(guid+table+priority=50), CleanTableTask(MoveTo table→WaitForCondition(!isDirty)→MoveTo trash→WaitForCondition(!dirty&&empty)), TableSet([RequireComponent GuidGenerator], GUID property, workerTargetPoint/WorkerTargetPosition, OnCustomerLeft→tableManager.PushDirtyTable, GetCleanedBy→tableManager.RemoveDirtyTable), TableManager(TaskRequester+dirtyTables list+PushDirtyTable+RemoveDirtyTable), Trash(workerTargetPoint+WorkerTargetPosition), WorkerManager(trash ref+HandleCleanTableRequest), Worker(CurrentTask property), Worker prefab: add HoldDishAbility
[x] Lesson 31: Priority-based task preemption — WorkerTask.Request property, HoldFoodAbility.IsPlateauActive, Worker.CanCancelTask(!IsPlateauActive), WorkerManager.HandleNoIdleWorkersFound: loop pending requests→loop workers→if CanCancelTask && pendingPriority>currentPriority→add current request back to pending→HandleRequest(new)→workerFound=true→break; if workerFound remove pending+break
[x] Lesson 32: Multi-worker + idle system — WorkerManager uses GetRandom() idle worker, IdleRequest(guid+pos+priority=-1), IdleTask(MoveTo+WaitForCondition(false)), WorkerIdlePosition([RequireComponent TaskRequester+GuidGenerator], 2s timer→CreateIdleRequest), WorkerManager handles IdleRequest, PlayerDetector: [SerializeField] NavigationAbility, OnTriggerStay returns if moving
[x] Lesson 33: Table cleaned event + task cancellation — TableManager.TableCleaned Action<TableSet,HoldDishAbility>, invoked in RemoveDirtyTable; WorkerManager subscribes+OnTableCleaned(remove pending CleanTableRequest by GUID, cancel worker's CleanTableTask if different cleaner); Worker.CancelTask(), WorkerTask.Cancel()(clears request from sender); CleanTableTask.Table property; Plateau.Push safety: if no empty position && childCount<maxCapacity → CreateNewFoodPosition before push
[x] Lesson 34: Highest-priority + closest worker — WorkerManager.GetHighestPriorityRequest()(int.MinValue loop→max priority index), GetTargetPositionFromRequest(FillStation→dropZonePos, ServeCustomers→workerTargetPos, CleanTable→table.WorkerTargetPosition), GetClosestWorker(idleWorkers,targetPos)(float.MaxValue loop→min distance index); HandleRequests uses these instead of pendingRequests[0]+GetRandom(). Unity: duplicate tables → each needs unique GUID (GuidGenerator → Generate GUID)
[x] Lesson 35: CashFile system — CashFile.cs(cashPrefab, Vector2Int gridSize, Vector3 gridSpacing, Vector3[] basePositions); Awake→StoreBasePositions(center grid: startPos=transform.pos - right*spacing.x*size.x/2 - forward*spacing.z*size.y/2 + gridSpacing/2, nested z/x loop, i=x+z*gridSize.x); GetTargetGridPosition(index)(elevationIndex=index/basePositions.Length, y=elevationIndex*spacing.y, baseIndex=index%length); GenerateCash(amount)(loop: targetPos=GetTargetGridPosition(childCount), Instantiate as child); [Button]GenerateOneCash(). Unity: CashFile empty GO child of Coffee Cashier Station + BoxCollider trigger, cashPrefab=cash prefab, gridSize=(2,4), gridSpacing=(0.75,0.15,0.4)
[x] Lesson 36: Cash collection + arc animation — ArcAnimator.cs(singleton, static Animate(Transform t,target,duration,delay,height,Action<GO> cb)→coroutine MoveAlongArc: WaitForSeconds(delay), timer loop: percent=timer/duration, pos=Lerp(start,target,pct), pos.y+=Sin(pct*PI)*height, final snap+cb.Invoke); CashFile: private int index, GenerateCash uses index+i for positioning + index+=amount, OnTriggerEnter(PlayerController→AnimateCashToPlayer+index=0), AnimateCashToPlayer(duration=2f,delayStep=duration/childCount,clamp 0.01f,loop reverse: ArcAnimator.Animate(cash,player,1f,delay,3f,HandleCashMovedAlongArc)), HandleCashMovedAlongArc→AddCurrency(2)+Destroy; FoodServingStation: +cashFile SerializeField, ServeFood→cashFile.GenerateCash(2) before Pop. Unity: ArcAnimator empty GO in scene, FoodServingStation inspector→assign Cash File
[x] Lesson 37: CashFile save/load — [RequireComponent GuidGenerator], IWantToBeSaved interface; private bool loaded; Awake→guidGenerator=GetComponent; Start: if(!loaded)Load(); GenerateCash(int amount, bool save=true): save=true→Save() at end, save=false used by Load(); OnTriggerEnter: after AnimateCashToPlayer+index=0 → Save(); Save()→Sijil.Save(this,guid,index); Load(): loaded=true, TryLoad(guid,out _index)→return if not found, else GenerateCash((int)_index, false). Unity: GuidGenerator auto-added by RequireComponent — generate unique GUID for each CashFile. CDO must be in scene (not child of another GO).
[x] Lesson 38: Locked element UI prefab — World Space Canvas(2x2, scale 1, rot X:90 Y:-90), Container(Image Square50), Fill(Image Filled Vertical Bottom, yellow), Outline(Image Square50 Outline), VerticalLayoutGroup, PriceText(TMP)+Icon(Image) with LayoutElement preferred heights, Anim parent for LeanTween. Unity only, no code.
[x] Lesson 39: LockedElement + PayAbility — LockedElement.cs(priceText+fillImage+anim+initialPrice, currentPrice, Awake→init price+text, OnTriggerEnter/Exit→ScaleUp/Down via LeanTween on anim, OnTriggerStay→check PayAbility+!isMoving→calculate cashPerFrame from animDuration+frameDuration→payAbility.Pay(cashPerFrame,this), CollectCash(amount)→currentPrice-=amount+UpdateVisuals+if<=0 Unlock, UpdateVisuals→fillAmount=1-current/initial+text, Unlock→Debug.Log); PayAbility.cs([RequireComponent CharacterController], cashPrefab, IsMoving→velocity.magnitude>0.1f, Pay(cashPerFrame,lockedElement)→toPay=Min(cashPerFrame,Currency)→AddCurrency(-toPay)→CollectCash(toPay)→AnimateCashTo, AnimateCashTo→Instantiate+disable collider+ArcAnimator.Animate→Destroy); CashFile.cs fix: disable collider on spawned cash prefabs. Unity: LockedElement script+BoxCollider(trigger 2x2x2) on Locked Element, PayAbility on Player+cash prefab, wire priceText+fillImage+anim+initialPrice=100.
[x] Lesson 40: LockedElement save/load + unlock — Save(guid,true) when unlocked, Save(guid+currentPriceKey,currentPrice) for partial; Load: TryLoad guid→Unlock else TryLoad currentPriceKey→UpdateVisuals; loaded bool prevents double-load in Start. TableManager: GetComponentsInChildren(includeInactive:true), activeInHierarchy check in GetFirstCleanEmptyTable.
[x] Lesson 41: Unity-only — NavMesh bake "Collect Objects: All Game Objects", Coffee Station + Cashier Station renders → NavMesh Obstacle (carve). LE prefabs: First Table LE, Coffee Station LE, Coffee Cashier Station LE (each in Prefabs/TableStuff/, unique GUID). Tools > Clear Save → Play to test locked/unlock flow.
[x] Lesson 42: ProgressionManager — ProgressionStep struct (name, List<LockedElement>, Hide/Display methods), IWantToBeSaved, progressionSteps[], progressionStepIndex, Start→Load (TryLoad index→hide all steps→display current step). Unity: Cooked Fast > Setup Lesson 42 → creates Progression Manager under MANAGERS, auto-wires 3 steps.
[x] Lesson 43: LockedElement event + ProgressionManager full wiring — LockedElement: static Action<LockedElement> Unlocked, Unlock(bool instant=false) invokes Unlocked+Save only if !instant, Load()→Unlock(instant:true). ProgressionManager: Awake/OnDestroy subscribe/unsubscribe LockedElement.Unlocked; ProgressionStep: Contains/UnlockElement/IsComplete (struct copy fix: reassign array element after UnlockElement); OnLockedElementUnlocked: Contains check→UnlockElement→IsComplete→progressionStepIndex++→Save→StartNextStep; Load() bug fix: hide loop starts from progressionStepIndex (not 0) so already-unlocked steps stay visible.
[x] Lesson 44: Progression camera + blocker canvas — ProgressionManager: CinemachineCamera progressionCamera + GameObject progressionCanvas, Awake→progressionCanvas.SetActive(false), StartNextStep→Display+StartCoroutine; StartStepCoroutine(step): new CameraTarget(), progressionCanvas=true, loop each lockedElement→set target.TrackingTarget+progressionCamera.Target+enable camera→WaitForSeconds(1f), after loop disable camera+canvas. Unity: duplicate Player Follow Camera→"Progression Camera" (clear tracking target, disable), Cinemachine Brain default blend In/Out 0.5s, Progression Canvas (sortOrder=5) + transparent Blocker panel (raycastTarget=true).
[x] Lesson 45: Unity-only office zone setup — Right Wall→NavMesh Obstacle (carve)→rebake NavMesh→disable wall. Office Zone GO under GAMEPLAY, walls (duplicated right wall, NavMesh Obstacle carve) as children. Office Ground (duplicate Main Ground, y=0.005, child of Office Zone). HR Elements + Player Upgrades Elements child GOs, each with: desk model (unpack prefab, Palette mat), trigger BoxCollider on desk + physical BoxCollider on renderer, monitors/chair props. Two manager NPCs (Worker prefab unpacked, remove: dish ability, character controller, player detector, hold food ability, worker, customer animator, nav agent), Manager Animator Controller (Sit anim looping), brown-ish Manager material. Office Zone disabled by default (unlocked later).
[x] Lesson 46: Office zone unlock wiring — LockedElement: [SerializeField] UnityEvent unlockedCallback (fires unconditionally in Unlock(), both instant-load and real-time unlock — for local/permanent effects like hiding a wall, unlike the static Unlocked Action which only fires on real-time unlock for progression camera flow). 3 new LockedElement instances: Office Zone LE (unlockedCallback→disable Right Wall GameObject), HR LE + Player Upgrades LE (nested inside Office Zone's Unlocked Elements, unlock the HR/Player Upgrades desk areas). Added as 3 more ProgressionManager steps after the original 3 (First Table, Coffee Station, Coffee Cashier Station). Unity: Cooked Fast > Setup Lesson 46 auto-finds "Office Zone LE"/"HR LE"/"Player Upgrades LE" by name and appends progression steps; Trash moved into Office Zone area + re-baked as NavMesh-carved. BUG FIX: OnLockedElementUnlocked guard `if (progressionStepIndex >= progressionSteps.Length) return;` added — prevented IndexOutOfRangeException when a LockedElement not included in any progression step fires Unlocked after all steps already completed.
[x] Lesson 47: HR Canvas UI — screen-space overlay canvas (sortOrder=1, disabled by default), Background panel, Title (TMP "Human Resources"), Back Button (Square_50 sprite red-ish + cross icon, anchored top-left (150,-142) to align with Currency Container's top-right (-138,-142)), Currency Container (copied from Main Canvas), Worker Container Scroll (ScrollRect vertical-only, Viewport+Mask, Content w/ VerticalLayoutGroup(padding 20, spacing 20, upperCenter, no forceExpand)+ContentSizeFitter(vertical=PreferredSize)), Worker UI Container prefab dropped as preview. Reused MatchOtherUIElement.cs (Scripts/Utilities/, snaps a RectTransform's position+size to a target RectTransform at runtime — used elsewhere for unlock-overlay buttons). Unity: Cooked Fast > Setup Lesson 47 → creates full HR Canvas scaffold; Progression Canvas sort order should stay above HR (10 recommended). BUG FIXES (session): Content Size Fitter Vertical Fit was Unconstrained instead of Preferred Size → Content never grew → ScrollRect had nothing to scroll; ScrollRect.m_ScrollSensitivity=1 too low for mouse wheel feel → raised to 20-30.
[x] Lesson 48: HR panel open/close logic — HRManager.cs(Managers/): CanvasGroup cg (serialized), IsPanelVisible=>cg.blocksRaycasts, Display()=>cg.Show(), Hide()=>cg.Hide() (uses existing Tabsil CanvasGroupExtensions.Show/Hide: alpha+blocksRaycasts+interactable). DeskStation.cs(Stations/): HRManager hrManager (serialized), bool hasEntered; OnTriggerStay: return early if panel visible/already entered/no PlayerController/player IsMoving()→otherwise hasEntered=true+hrManager.Display(); OnTriggerExit: PlayerController check→hasEntered=false (does NOT hide panel — only the Back Button's onClick, wired to HRManager.Hide via persistent listener, closes it). Unity: Cooked Fast > Setup Lesson 48 → adds CanvasGroup to HR Canvas (starts hidden), creates HR Manager under MANAGERS wired to CanvasGroup, adds DeskStation to HR Desk wired to HR Manager, wires Back Button onClick→HRManager.Hide. BUG FIX: GameObject.Find skips inactive objects — HR Desk (nested in disabled Office Zone) and Office Zone LE's nested HR LE/Player Upgrades LE weren't found by Setup Lesson 39/46/48; switched to FindObjectsByType<GameObject>(FindObjectsInactive.Include, ...) across all three.
[x] Lesson 49: Worker data + dynamic container spawning — WorkerDataSO.cs(Data/, ScriptableObject, CreateAssetMenu): name(new keyword to hide Object.name), unlockPrice, initialLevel, profilePicture(Sprite), prefab(Worker) + read-only properties region. UIWorkerContainer.cs(UI/): Initialize(HRManager hrManager, WorkerDataSO data, int workerLevel) stores refs (visual configuration deferred to next lesson). HRManager.cs: uiWorkerContainerPrefab(UIWorkerContainer) + workerContainersParent(Transform) + workerDatas[](WorkerDataSO) + List<UIWorkerContainer> workerContainers; Start()→GenerateWorkerContainers() loops workerDatas, Instantiate(prefab, parent), containerInstance.Initialize(this, data, workerLevel:0), adds to list. Unity: Cooked Fast > Setup Lesson 49 → adds UIWorkerContainer component to Worker UI Container.prefab, creates 5 WorkerDataSO assets (Data/Workers/: Angelo 100/lvl0, Kai 200/lvl1, Jawed 300/lvl2, Matteo 400/lvl3, Ethan 500/lvl4) with shared Worker_Icon.png + Worker.prefab, wires HR Manager's prefab/parent(Content)/workerDatas[], removes leftover manual preview card from Content.
[x] Lesson 50: UIWorkerContainer visual configuration — UIWorkerContainer.cs gains Elements(profileImage Image, nameText TMP, lockedOverlay GameObject), Buttons(unlockButton, videoUnlockButton, upgradeButton, videoUpgradeButton), Texts(unlockPriceText, upgradePriceText). Initialize() now also sets profileImage.sprite + nameText.text, then InitializeButtonCallbacks() (4 lambdas forwarding to hrManager.OnContainerXxxButtonClicked(this) — button LOGIC lives in HRManager, not the container, to keep it in one place) + UpdateButtonVisuals(). UpdateButtonVisuals(): lockedOverlay.activeInHierarchy is the locked/unlocked test → if locked, UpdateUnlockButton() and return; else upgradePriceText="<sprite=0> 100" + upgradeButton.interactable=HasEnoughCurrency(100) (hardcoded 100 for now, level-based calc comes next lesson). UpdateUnlockButton(): transform.GetSiblingIndex()<=0 → first container is FREE + always interactable; otherwise "<sprite=0> "+workerData.UnlockPrice and interactable=CurrencyManager.instance.HasEnoughCurrency(UnlockPrice). HRManager: 4 empty public OnContainerXxxButtonClicked(UIWorkerContainer) stubs. Unity: Cooked Fast > Setup Lesson 50 wires all 9 prefab refs by deep name search (price texts pulled via GetComponentInChildren<TMP> on their button). NOTE: HR Canvas needs blocksRaycasts+interactable on for buttons to respond.
[x] Lesson 51: Worker unlock + spawn — HRManager: [SerializeField] WorkerManager workerManager + private int[] workerLevels (parallel to workerDatas); GenerateWorkerContainers allocates workerLevels and seeds every entry to -1 (negative = locked, Load() overwrites in a later lesson), passing workerLevels[i] into Initialize instead of the old hardcoded 0. OnContainerVideoUnlockButtonClicked(uiWorkerContainer): uiWorkerContainer.Unlock() → workerIndex=GetSiblingIndex() → workerLevels[workerIndex]=workerDatas[workerIndex].InitialLevel → workerManager.SpawnWorker(data, level) → // Save (next lesson). UIWorkerContainer: workerLevel field renamed to level, set right after InitializeButtonCallbacks; public Unlock() → lockedOverlay.SetActive(false) + level=Mathf.Max(workerData.InitialLevel, level). WorkerManager: [SerializeField] Transform workerSpawnPoint + public SpawnWorker(WorkerDataSO, int workerLevel) → Instantiate(data.Prefab, spawnPoint.position, identity, transform), worker.name=data.Name (otherwise it inherits the SO asset name), workers.Add, worker.Initialize(this, level). Worker: private WorkerManager workerManager + private int level + public Initialize(WorkerManager, int) → stores both then SetupStats() (empty stub; speed/capacity/revenue land later). Unity: Cooked Fast > Setup Lesson 51 creates a "Worker Spawn Point" GameObject (NOT parented to Worker Manager, since spawned workers get parented there) and wires WorkerManager.workerSpawnPoint + HRManager.workerManager. Only the blue FREE (video) button spawns workers this lesson; the paid unlock button stays empty. Nothing persists yet — workers vanish on restart until Lesson 52 adds save/load.
[x] Lesson 52: Worker unlock save/load — HRManager implements IWantToBeSaved (using Tabsil.Sijil), const workerLevelKey="worker_level_". Save(): loops workerLevels writing Sijil.Save(this, workerLevelKey+i, workerLevels[i]) — the level alone encodes BOTH unlocked state and upgrade progress since negative = locked, so no separate "isUnlocked" flag is needed. Load(): allocates workerLevels, per index TryLoad(workerLevelKey+i) → cast to int, else -1; then GenerateWorkerContainers() + workerManager.Initialize(workerDatas, workerLevels). Start() REMOVED — container generation moved into Load(), which Sijil invokes on its sceneLoaded handler (after every Awake, before every Start), so WorkerManager.Awake has already run. GenerateWorkerContainers no longer allocates/seeds workerLevels (Load owns that now). OnContainerVideoUnlockButtonClicked's `// Save` comment became a real Save() call. WorkerManager.Initialize(WorkerDataSO[], int[]): loops levels, `if (workerLevels[i] < 0) continue;` else SpawnWorker(data, level) — note level 0 DOES spawn (Angelo's initialLevel is 0), only negatives are skipped. No Unity setup needed. KNOWN GAP at this point in the course: on load the workers respawn in the scene but their UI cards still render locked (Initialize never calls Unlock()), so the panel can unlock an already-unlocked worker and spawn a duplicate — the course addresses the container UI in a later lesson.
[x] Lesson 53: Paid unlock + upgrade plumbing + blob scaffolding — HRManager.OnContainerUnlockButtonClicked mirrors the video version but adds CurrencyManager.instance.AddCurrency(-workerDatas[i].UnlockPrice); deliberately NOT delegating to the video method because that one grows ad logic later. No affordability check needed — UIWorkerContainer.UpdateUnlockButton already gates interactable on HasEnoughCurrency. OnContainerVideoUpgradeButtonClicked: workerLevels[i]++ → uiWorkerContainer.LevelUp() → workerManager.LevelUpWorker(uiWorkerContainer.WorkerName) → Save(). UIWorkerContainer gains: UIWorkerStat[] stats, `public string WorkerName => workerData.Name`, and LevelUp() (level++ then UpdateButtonVisuals). WorkerManager.LevelUpWorker(string) is an empty stub this lesson (workers are named from their SO in SpawnWorker, so the name is a valid lookup key). NEW UIWorkerStat.cs(UI/): upgradeBlobPrefab + blobsParent + List<UIUpgradeBlob> blobs + blinkingBlobIndex; Initialize(maxBlobCount, activeBlobCount) instantiates maxBlobCount blobs into blobsParent and calls Activate() on the first activeBlobCount of them, then blinkingBlobIndex=activeBlobCount. NEW UIUpgradeBlob.cs(UI/): blobImage + activeColor, Activate() tints the image. BLOB LEVEL SCHEME: one shared level drives three stat rows in column order — lvl1 fills Speed blob 1, lvl2 Capacity blob 1, lvl3 Revenue blob 1, lvl4 Speed blob 2, etc. (so a row's active count goes 1, then +3 each cycle). Unity: Cooked Fast > Setup Lesson 53 adds UIUpgradeBlob to Worker Upgrade Blob.prefab (wiring its child Image), adds UIWorkerStat to Speed/Capacity/Revenue Section with blobsParent = each section's Horizontal Layout row, and fills container.stats[]. NOT YET WIRED: nothing calls UIWorkerStat.Initialize, so no blobs spawn at runtime — the transcript ends saying worker max-stats must be added first, which is Lesson 54.
[x] Lesson 54: Worker stats + blobs go live — WorkerDataSO gains [Range(2,8)] maxSpeed/maxCapacity/maxRevenue + Max* properties. IMPORTANT: keep all three EQUAL per worker; the blob/level math assumes a shared max (course disclaimer). UIWorkerContainer.Initialize now calls InitializeStats() then branches on the level instead of the overlay: `if (level < 0) { UpdateUnlockButton(); return; }` else `Unlock()` — this finally fixes the Lesson-52 gap where a saved-unlocked worker still rendered locked. InitializeStats(): statLevels = GetStatLevelsFromLevel(level), then stats[0/1/2].Initialize(MaxSpeed/MaxCapacity/MaxRevenue, statLevels.x/y/z). GetStatLevelsFromLevel(level) returns Vector3Int with speed=CeilToInt(Max(0f,level)/3f), capacity=CeilToInt(Max(0f,level-1)/3f), revenue=CeilToInt(Max(0f,level-2)/3f) — the Max(0f,..) guards the negative offsets at low/locked levels and the f-suffixes force float division. Verify: lvl1→(1,0,0), lvl2→(1,1,0), lvl3→(1,1,1), lvl4→(2,1,1). UIWorkerContainer.LevelUp() implemented: statIndex = level % stats.Length → stats[statIndex].Increment() → level++ → UpdateButtonVisuals() (Increment uses the PRE-increment level, which is what makes the column order work). UIWorkerStat.Increment(): blobs[blinkingBlobIndex].Activate() then blinkingBlobIndex++. Unity: Cooked Fast > Setup Lesson 54 writes equal stat tiers per worker (Angelo 2, Kai 3, Jawed 4, Matteo 5, Ethan 7). TWO KNOWN BUGS the course leaves for the next lesson: (1) UIWorkerStat.Increment throws IndexOutOfRange once a row is maxed — the upgrade needs clamping; (2) the upgrade button still shows the hardcoded 100 placeholder instead of a real level-based price.
[x] Lesson 55: Upgrade clamping, pricing, currency refresh — a fix-heavy lesson, four separate problems. (a) MAX CLAMP: UpdateButtonVisuals computes maxLevel = workerData.MaxSpeed*3 (the combined ceiling across the three rows — only valid because the three maxes are equal); if maxed, upgradePriceText shows "max", upgradeButton.interactable requires HasEnoughCurrency(price) && !isMaxed, and videoUpgradeButton.gameObject is hidden entirely rather than merely disabled. This kills the IndexOutOfRange from Lesson 54. (b) REAL PRICE: new static HRManager.GetWorkerUpgradePriceFromLevel(level) => 10 + Mathf.Max(level,0)*20 (10 base, +20 per level), replacing the hardcoded 100; the paid upgrade path OnContainerUpgradeButtonClicked charges it with AddCurrency(-price) computed from the level BEFORE the increment so it matches the number on the button, then mirrors the video-upgrade flow (level++, LevelUp, LevelUpWorker, Save). (c) UNLOCK ORDERING BUG: at startup UpdateButtonVisuals ran while the overlay was still active, so a freshly-unlocked container kept showing the locked branch — fixed by calling UpdateButtonVisuals() at the END of Unlock(), after lockedOverlay.SetActive(false). (d) BLOB INIT BUG: InitializeStats used the raw level, which is -1 while locked, so a worker with a non-zero InitialLevel showed no earned blobs; fixed with GetStatLevelsFromLevel(Mathf.Max(workerData.InitialLevel, level)). (e) CURRENCY REFRESH: UpdateButtonVisuals became public; HRManager subscribes to CurrencyManager.updated in Awake / unsubscribes in OnDestroy, and OnCurrencyUpdated loops workerContainers calling UpdateButtonVisuals — plus Display() calls OnCurrencyUpdated() too, since the wallet can change while the panel is closed and the initial currency load can land after the containers are built. Added a `workerContainers == null` guard in OnCurrencyUpdated (not in the transcript) because the event can fire before Load() builds the list. No Unity setup. Next lesson finally APPLIES the stats — workers still all move at the same speed.
[x] Lesson 56: Stats finally applied to the workers — NEW BaseCharacterStatsSO.cs(Data/): [Range(1,4)] float speed, [Range(7,14)] int capacity, [Range(1,2)] float revenue + Speed/Capacity/Revenue properties. NEW CharacterStats.cs(Gameplay/): a MonoBehaviour meant for ANY stat-driven character (workers now, the player later) — serialized baseStats (BaseCharacterStatsSO), private speed/capacity/revenue fields seeded from the base values in Awake, and SetupStats(Vector3Int statsLevels) which recomputes each as base + CalculateAdditionalX(level): speed adds level*.2f, capacity adds level (so lvl1 with base 7 → 8), revenue adds level*.2f — the transcript says level*2f, but that pays a maxed worker 15 bills per cup against an unupgraded worker's 1; the video's on-screen 1.6 multiplier was hand-typed into the Debug inspector, not computed, so the formula is never actually tested there. With .2f a maxed Ethan lands at 2.4 → 3 bills. Same file also holds `public static class WorkerUtilities` with GetStatsLevelsFromLevel(int) — the exact Vector3Int ceil-division math MOVED OUT of UIWorkerContainer (which now calls WorkerUtilities.GetStatsLevelsFromLevel and no longer has its own copy), because the workers need the same conversion. Course tip: a second class can live at the bottom of an existing script when it's this small. Worker.cs: adds [RequireComponent(typeof(CharacterStats))], characterStats = GetComponent<CharacterStats>() in Awake, a private float revenueMultiplier field, and its previously-empty SetupStats() now does levels = WorkerUtilities.GetStatsLevelsFromLevel(level) → characterStats.SetupStats(levels) → navigationAbility.SetSpeed(characterStats.Speed), holdFoodAbility.SetPlateauCapacity(characterStats.Capacity), revenueMultiplier = characterStats.Revenue. Three new pass-through methods down the chain: NavigationAbility.SetSpeed(float) sets agent.speed; HoldFoodAbility.SetPlateauCapacity(int) calls plateau.UpdateMaxCapacity(capacity); Plateau.UpdateMaxCapacity(int) just assigns maxCapacity (deliberately NOT resetting isFull — this only runs during stat setup while the plateau is empty). Unity: Cooked Fast > Setup Lesson 56 creates Data/Base Stats/Worker Base Stats.asset (speed 2 = the NavMeshAgent default, capacity 7, revenue 1 — values written only on first creation so re-running never clobbers Inspector tuning), adds CharacterStats to Prefabs/Characters/Worker.prefab and wires baseStats. One base-stats asset is shared by all workers; per-worker assets are possible later. DEVIATION FROM THE COURSE (we implemented it early, the transcript leaves it as a known limit): WorkerManager.LevelUpWorker now actually finds the worker by name and calls a new Worker.LevelUp() (level++ then SetupStats() again), so an upgrade reaches the NavMeshAgent and the plateau immediately instead of only after a restart. Without it, a worker keeps its spawn-time stats for the whole session — e.g. Ethan showed 2.4 speed (his initialLevel 5 → speed level 2) no matter how many upgrades were bought. Revenue is stored but unused until the station takes it into account (next lesson).
[x] Lesson 57: Runtime stat refresh + revenue applied — the lesson's FIRST HALF (WorkerManager.LevelUpWorker looping workers by gameObject.name and calling a new Worker.LevelUp() = level++ then SetupStats()) was already implemented at the end of our Lesson 56 session, so nothing to redo; the course's approach matches ours exactly. Course note: two workers must never share a name, since the name IS the lookup key (SpawnWorker assigns worker.name = workerDataSO.Name); alternatives would be an index or storing the WorkerDataSO on the Worker. SECOND HALF — REVENUE: FoodServingStation.OnTriggerStay now does `if (!other.TryGetComponent(out CharacterStats characterStats)) return;` and threads characterStats through HandleFoodServing(characterStats) → ServeFood(characterStats). ServeFood replaces the hardcoded cashFile.GenerateCash(2) with: baseRevenue = 1, revenueMultiplier = Mathf.Max(1f, characterStats.Revenue), revenue = Mathf.CeilToInt(baseRevenue * revenueMultiplier), GenerateCash(revenue). The Max(1,..) floor means an un-upgraded worker still earns; CeilToInt means a 1.6 multiplier already pays 2. IMPORTANT SIDE EFFECT (not in the transcript, we handled it): that early return also blocks the PLAYER from serving, because only the Worker prefab had CharacterStats — so Setup Lesson 57 creates Data/Base Stats/Player Base Stats.asset and adds CharacterStats to the scene Player object. This matches the course's own stated intent that CharacterStats is for "the workers and the player". CASH PILE FIX: collecting cash while more was being generated threw a missing-reference error from ArcAnimator (the pile's children changed under the running coroutines). CashFile.AnimateCashToPlayer now does `cash.parent = null;` inside the loop right before ArcAnimator.Animate, so in-flight bills leave the pile. TWO ADDITIONS OF OURS on top of the transcript's fix: (1) childCount is cached into `cashCount` BEFORE the loop — unparenting shrinks childCount live, which would have collapsed every stagger delay to 0 and made all the cash fly at once; (2) HandleCashMovedAlongArc returns early on a null cash, since ArcAnimator explicitly invokes the callback with null when the transform vanished mid-flight, and without the guard that still paid out currency. Course mentions a cleaner long-term option: give the pile two child containers, one for collectable cash and one for cash still being generated. Revenue-per-bill is still a flat AddCurrency(2) in HandleCashMovedAlongArc — the multiplier changes the NUMBER of bills, not their value.
[x] Lesson 58: Blinking "next upgrade" blob — a short UI-only lesson. UIUpgradeBlob gains a serialized blinkColor and a Blink(): LeanTween.cancel(blobImage.gameObject), blobImage.color = Color.white, then LeanTween.color(blobImage.rectTransform, blinkColor, 1f).setLoopPingPong(-1) for an endless ping-pong. CRITICAL DETAIL the course hits as a live bug: Activate() must ALSO call LeanTween.cancel(blobImage.gameObject) — cancelling the blob's own gameObject instead of the IMAGE's leaves the tween running, so an upgraded blob keeps blinking over its active color. Cancel target must match whatever Blink() tweened. UIWorkerStat.Blink() guards the maxed row with `if (blinkingBlobIndex > blobs.Count - 1) return;` then calls blobs[blinkingBlobIndex].Blink(). UIWorkerContainer calls it in two places: at the end of InitializeStats, and in LevelUp AFTER level++ so the hint moves to the next row in the rotation (the Increment() call still uses the pre-increment index — same trick as Lesson 54). OUR FIX on top of the transcript: InitializeStats now computes `displayLevel = Mathf.Max(workerData.InitialLevel, level)` once and uses it for BOTH the stat levels and the blink index — the transcript uses the raw level for the modulo, which is -1 for a locked worker, and C# gives -1 % 3 == -1, i.e. an IndexOutOfRange on stats[]. Unity: set Blink Color on Prefabs/UI/Worker Upgrade Blob.prefab (yellow/red, fully subjective). LeanTween already ships in Assets/LeanTween. Next lesson: player upgrades in the office, which the course says reuses the worker logic almost verbatim.
[ ] Lesson 59: Player Upgrades Canvas — ZERO C# this lesson, it is pure Editor layout work; the course explicitly rushes it ("you already know how to do that, we've done it for the workers"). Hierarchy built by Cooked Fast > Setup Lesson 59: Player Upgrades Canvas (ScreenSpaceOverlay, sortOrder 1, ScaleWithScreenSize 1080x1920, Match=Width like the other panels) > PANEL (full-stretch dark overlay image) > Container (bottom-anchored sheet, height 800, VerticalLayoutGroup; its Image is KEPT ENABLED but fully transparent purely to stay a raycast target — the course's exact reasoning) > Top Ribbon (red-ish, LayoutElement preferredHeight 140, holds a centered "PLAYER" TextMeshProUGUI and a Close Button borrowed from the HR back button, right-anchored, callback deliberately left EMPTY so nothing misfires) + Upgrade Buttons Container (LayoutElement flexibleHeight 1, HorizontalLayoutGroup spacing 50, childControl/ForceExpand ALL OFF so the cards keep their own size — the course tries expanding and rejects it because it looks wrong on iPad). UI Player Upgrade Container.prefab (already in the project, has Upgrade Title Section / Icon Section / Upgrade Blobs Section / Free Upgrade Button Section) gets resized to 260x450. The course places three cards to eyeball the layout then DELETES them, because they get spawned at runtime like the worker containers — so the setup script places none. Player gets 5 levels per stat (speed/capacity/revenue) per the course. Canvas ships with a CanvasGroup at alpha 0 / interactable false / blocksRaycasts false, GameObject left ACTIVE, so Lesson 60 only has to drive the group. Course tip: check the layout in Simulator view, not just Game view; if you need per-device behaviour, script an iPad check that disables expansion and fixes the container width.

== SESSION BUG FIXES (Lesson 29-32 arası) ==
- Plateau.Start()→Awake(): disabled plateau ilk SetActive(true)'de Start() bir frame geç çalışıp isEmpty=true reset ediyordu → dirty dish dump çalışmıyordu
- Trash.OnTriggerExit: HoldDishAbility kontrolü eklendi (worker geçince timer sıfırlıyordu)
- Trash.OnTriggerStay: HasDishes=false'ta dumpTimer=0 kaldırıldı (worker'ın boş plateau'su timer'ı resetliyordu)
- PlayerDetector: NavigationAbility auto-get Awake'te (duplicate worker'larda null kalıyordu)
- PlayerDetector: IdleTask kontrolü eklendi (idle pozisyonda food almasın)
- Worker.CanCancelTask: IdleTask ise her zaman true (plateau aktif olsa bile)
- IdlePositions ≥ Worker sayısı olursa 1 request hep pending'de kalır — bu beklenen davranış, bug değil

== BUG FIXES (session arası) ==
- Trash: OnTriggerEnter→OnTriggerStay+timer(0.2s)+OnTriggerExit reset
- GuidGenerator: Tabsil kütüphanesinde mevcut, bizimki silindi — GUID property adı: .GUID (büyük harf)
- Worker physics layer: ayrı layer'a alındı, Player ile çarpışma kapatıldı (Layer Collision Matrix)
- HoldDishAbility: artık [RequireComponent HoldFoodAbility], plateau = GetComponent<HoldFoodAbility>().Plateau (aynı plateau, inspector hatası önlendi)
- Worker.holdFoodAbility: SerializeField kaldırıldı, Awake'te GetComponent ile alınıyor
- Plateau.Start()→Awake(): disabled plateau ilk SetActive(true)'de Start() bir frame geç çalışıp isEmpty=true reset ediyordu; Awake'e alınca senkron oldu (dirty dish dump bug)
- Trash.OnTriggerExit: HoldDishAbility kontrolü eklendi — worker geçince timer reset etmiyordu
- Trash.OnTriggerStay: HasDishes=false'ta dumpTimer=0 kaldırıldı — worker'ın boş plateau'su timer'ı resetliyordu
